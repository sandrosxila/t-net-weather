# T-NET Weather App

## Requirements

- node v20.12.0
- yarn 1.22.19

## Set Up

use `lts/iron` with `nvm`:

```bash
nvm use 20
```

install dependencies:

```bash
yarn
```

run locally

```bash
yarn dev
```
